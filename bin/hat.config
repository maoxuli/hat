#
# The client reads this property to create the reference to the
# "profile" object in the server
# "filter" object in the server
#
Synchronizer.Proxy=synchronizer:tcp -p 10001:udp -p 10001
Filter.Proxy=filter:tcp -p 10000:udp -p 10000