#
# The server creates one single object adapter with the name
# "HatFile". The following line sets the endpoints for this
# adapter.
#
HatSync.Endpoints=tcp -p 10001:udp -p 10001

#
# The client reads this property to create the reference to the
# "profile" object in the server
# "filter" object in the server
#
Profile.Proxy=profile:tcp -p 10000

#
# Default path of files
#
Files.Path=/Users/Shared/Photos

#
# Host name
#
Host.Name=maoxu